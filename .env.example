# Copy to .env and export before running outside Docker:
#   set -a && . ./.env && set +a
#
# docker compose injects these itself -- see docker-compose.yml.

DB_HOST=localhost
DB_PORT=5432
DB_NAME=robot_scheduler
DB_USER=user
DB_PASSWORD=password
